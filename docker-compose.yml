services:
  db:
    image: postgres:15-alpine
    restart: always
    ports:
      - "5432:5432"

  backend:
    build: ./backend
    depends_on:
      - db

  frontend:
    build: ./frontend
    depends_on:
      - backend
